#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Alphabet Game
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
